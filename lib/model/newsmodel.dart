class NewsModel{

  var title , description, url, urlToImage, name, publishedAt ;

  NewsModel({
    required this.title,
    required this.description,
    required this.url,
    required this.urlToImage,
    required this.name,
    required this.publishedAt,
  });
}